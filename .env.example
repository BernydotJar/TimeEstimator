# Local Genkit mode (optional)
# GOOGLE_GENAI_API_KEY=

# n8n AI mode (GitHub Pages-compatible)
# Provide either the base URL OR explicit per-operation URLs.
# These endpoints are public in a static deployment. Do not place secrets here.
NEXT_PUBLIC_N8N_WEBHOOK_BASE_URL=
NEXT_PUBLIC_N8N_ANALYZE_ESTIMATE_URL=
NEXT_PUBLIC_N8N_ESTIMATE_DEFAULTS_URL=
NEXT_PUBLIC_N8N_SUMMARIZE_ACTIVITIES_URL=
NEXT_PUBLIC_N8N_PARSE_STEPS_URL=
