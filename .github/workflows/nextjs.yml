name: Deploy Next.js site to GitHub Pages

on:
  push:
    branches: ["main", "feat/**", "feature/**", "fix/**"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci --prefer-offline

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Build static export
        run: npm run build
        env:
          GITHUB_ACTIONS: true
          NEXT_PUBLIC_N8N_WEBHOOK_BASE_URL: ${{ vars.NEXT_PUBLIC_N8N_WEBHOOK_BASE_URL }}
          NEXT_PUBLIC_N8N_ANALYZE_ESTIMATE_URL: ${{ vars.NEXT_PUBLIC_N8N_ANALYZE_ESTIMATE_URL }}
          NEXT_PUBLIC_N8N_ESTIMATE_DEFAULTS_URL: ${{ vars.NEXT_PUBLIC_N8N_ESTIMATE_DEFAULTS_URL }}
          NEXT_PUBLIC_N8N_SUMMARIZE_ACTIVITIES_URL: ${{ vars.NEXT_PUBLIC_N8N_SUMMARIZE_ACTIVITIES_URL }}
          NEXT_PUBLIC_N8N_PARSE_STEPS_URL: ${{ vars.NEXT_PUBLIC_N8N_PARSE_STEPS_URL }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./out

  deploy:
    if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
